#
# Sniffer USB Manager - best setup
#
# BEST WAY (strongly recommended):
#   Open the GUI (double-click Desktop icon) and click the big
#   "🚀 Full Optimized Setup (Recommended)" button.
#
#   This does BOTH passwordless sudo + udev rules in one go.
#
# MANUAL passwordless sudo only:
#   sudo visudo -f /etc/sudoers.d/sniffer-usb-manager
#   Paste:
will ALL=(root) NOPASSWD: /home/will/bin/sniffer-usb-manager/priv-helper.sh

# (The GUI Full Setup button also installs excellent udev rules for
#  plug-and-play behavior with your Ubertooth One + Raytac MDBT50Q-CX + HackRF hardware.)
